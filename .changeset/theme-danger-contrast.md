---
"@plainworks/theme": patch
"@plainworks/mocks": minor
---

Improve theme accessibility and browser integration: darken the light-mode danger role for WCAG 2.2 AA contrast, declare the native light/dark color scheme, and minimize transition and animation durations when reduced motion is requested. Add type-safe nullable update fields to mock input decoding and normalize null clears across CRUD PATCH updates.

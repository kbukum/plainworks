// Fixture: a stand-in `theme` (L1) surface. `state` (also L1) importing this is a sideways
// (same-layer) import the gate must catch; nothing may flag `theme` itself (it imports nothing).
export const token = "token"

---
"@plainworks/ui": patch
---

`Callout` colors its `info`, `success`, and `warning` tones itself, so it no longer depends on extra `Alert` variants. The breadcrumb current page follows the atom and is exposed as a disabled link with `aria-current="page"`.

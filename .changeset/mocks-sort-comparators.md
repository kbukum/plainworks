---
"@plainworks/mocks": patch
---

Add `sortComparators` to `createCrudHandlers` and `comparator` to `sortBy` in `@plainworks/mocks`, allowing domain-specific value comparators (such as priority rankings) when sorting collections.

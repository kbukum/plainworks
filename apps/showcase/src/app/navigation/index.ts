// Re-export-only barrel for the app's section information architecture.
export type { Crumb, Section, SectionId } from "./sections"
export { breadcrumbTrail, NOTIFICATIONS_PATH, SECTIONS, sectionForPath } from "./sections"

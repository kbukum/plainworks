{
  "name": "@plainworks/showcase",
  "version": "0.0.0",
  "description": "Reference application: a server-rendered React dashboard that assembles the published @plainworks/* surfaces the way a consumer does — composition kernel, theme, query, state, and ui — over the mock backend. Never published; nothing depends on it.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run server.ts",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "e2e": "playwright test"
  },
  "dependencies": {
    "@plainworks/app": "workspace:*",
    "@plainworks/auth": "workspace:*",
    "@plainworks/demo": "workspace:*",
    "@plainworks/elements": "workspace:*",
    "@plainworks/http": "workspace:*",
    "@plainworks/mocks": "workspace:*",
    "@plainworks/query": "workspace:*",
    "@plainworks/state": "workspace:*",
    "@plainworks/std": "workspace:*",
    "@plainworks/theme": "workspace:*",
    "@plainworks/ui": "workspace:*",
    "@tanstack/react-query": "catalog:",
    "lucide-react": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@axe-core/playwright": "catalog:",
    "@plainworks/testkit": "workspace:*",
    "@playwright/test": "catalog:",
    "@tailwindcss/vite": "catalog:",
    "@testing-library/dom": "catalog:",
    "@testing-library/react": "catalog:",
    "@testing-library/user-event": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",

    "jsdom": "catalog:",
    "msw": "catalog:",
    "tailwindcss": "catalog:",
    "tw-animate-css": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}

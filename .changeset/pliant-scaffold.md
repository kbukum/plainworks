---
---

Set up the plainworks monorepo: workspaces, the task runner and package generator, the build preset, linting, the layer and version checks, changesets, and CI, plus the architecture doc. No feature packages yet.

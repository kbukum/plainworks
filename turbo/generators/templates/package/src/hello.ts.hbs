/**
 * Placeholder concern module for `@plainworks/{{name}}` — server-safe (no React or DOM imports,
 * runs anywhere: Node, edge, RSC). Replace with the package's real surface, one module per concern.
 */
export function hello(name: string): string {
  return `hello, ${name}`
}

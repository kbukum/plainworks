---
"@plainworks/elements": patch
"@plainworks/ui": patch
---

Expand `@plainworks/ui` with high-level application components, view-state behaviour hooks, and a controlled data table:

- Build accessible application interfaces with ready-made components for layout, feedback (`Callout`, `SkeletonText`), overlays (`Modal`, `Drawer`), value formatting (`DateValue`), and navigation (`Breadcrumbs`), importable directly from `@plainworks/ui/client` or modular subpaths.
- Drive interactive UI state with dedicated behaviour hooks (`useSelection`, `useControllableState`, `useClipboard`, `useKeyboardShortcuts`).
- Present tabular data with `DataTable`, providing sortable columns, row selection, responsive column visibility, and built-in loading and empty states while keeping row data and sorting fully caller-controlled.
- Access atomic primitives directly from `@plainworks/ui/client` alongside higher-level composite components.

"use client"

// Re-export-only barrel for the layout primitives (stack · grid · split).
export type { Gap, GridProps, SplitProps, StackProps } from "./layout"
export { Grid, Split, Stack } from "./layout"

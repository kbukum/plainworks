// Fixture: a stand-in `testkit` (L4) surface — shared test fakes. A *test* file in a lower package
// may import this (the one upward exception); a *production* file may not.
export const fake = "fixture"

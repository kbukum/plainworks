// Fixture: a stand-in `ui` `data` concern (band 3). `forms` (band 2) importing it is an upward
// concern import the ui-internal-direction gate must catch; this file imports nothing itself.
export const dataTable = "data-table"

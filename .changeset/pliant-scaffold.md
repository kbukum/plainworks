---
---

Scaffold the plainworks monorepo: bun workspaces + catalog, Turborepo pipelines and `turbo gen` package generator, tsdown build preset (ESM-only, per-module `"use client"`, ships `dist`), Biome, dependency-cruiser layer/cycle gate, Sherif + Syncpack version sync, Changesets, CI (SHA-pinned, Node 22/24), and the architecture doc. No feature packages yet.

// Fixture: a stand-in `elements` (L2) surface. `theme` (L1) importing this is an upward import the
// gate must catch; nothing may flag `elements` itself (it imports nothing).
export const atom = "atom"

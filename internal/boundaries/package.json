{
  "name": "@plainworks/boundaries",
  "version": "0.0.0",
  "description": "Layer-boundary + cycle gate (dependency-cruiser). Isolates the TS-AST toolchain so a future TypeScript 7 migration aliases `typescript` here only.",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "dependency-cruiser --config .dependency-cruiser.cjs ../../packages ../../apps ../../internal",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "dependency-cruiser": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}

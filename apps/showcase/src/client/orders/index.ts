"use client"

// Re-export-only barrel for the Orders section — the read-first orders catalog with a rich detail
// overlay and an authorized status change.
export { OrdersSection } from "./orders-section"
